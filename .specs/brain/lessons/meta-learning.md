# Meta-Learning Log — hitl-channel

This log tracks the results of the Empire Self-Reflection Protocol across sessions.

- 2026-05-18 | what:slaser79/hitl-channel how:~/.claude | session:SPEC-HITL-CC-001 Phase 4 AC#26 ReplyBuffer ship (PR #11) + reflection | direct:4 issues:0 | recurrences:1 (Shipped per-instance ReplyBuffer with peek+commit drain, monotonic sequence sort, wsSendAccepted helper applied across all WS send sites, pre-cached raw payload — 4 review cycles, 5 commits incl. openhands' parallel auto-fix, 2× clean Approve on R4, admin-merged after CEO HITL. Declined R2 P2#2 and R3 P1 (per-WS routing — same finding repackaged) as groundwork with §3.8/§7(b) rationale. Reflection added four direct edits to `~/.claude/skills/empire-implement/SKILL.md`: §1.0 inline-modifier vocabulary table (recognise `no worktree` / `note <directive>` / `stack on PR #N` / `skip e2e` / `use Plan first` as protocol input, not ambiguity prompts) prevents the U1 disease that opened the session with a "breaking empire protocol" interrupt; §1.5-runtime extends the contract-probe doctrine to vendored runtime primitives (Bun ws.send, Deno serve, Node ws, Dart sink, Python asyncio writer) — same disease class as PR #3719 IANA Etc/GMT (recurrence n=1, lesson-without-enforcement); §6.0.5 pre-push parallel-agent check (`git fetch origin <branch> && git log --oneline HEAD..origin/<branch>` before every fix push) prevents openhands push-collision recovery loops; §6.4a intra-cycle bot disagreement extends §6.4's cross-cycle contradiction doctrine to same-SHA simultaneous-reviewer disagreement (treat each finding independently, record both verdicts in status comment, don't escalate the split itself). §9 known-traps table grew a "Vendored WS / socket send return values" entry. All four Ugly items were doctrine gaps, not project-specific conventions — passed the §5.5 projects-test for skill placement.)
